query GetLearningMaterials($limit: Int, $offset: Int) {
	learningMaterials(limit: $limit, offset: $offset) {
		id
		ownerId
		key
		name
		status
		sourceUrl
		contentType
		contentId
		createdAt
		updatedAt
	}
}
