query GetClients($limit: Int, $offset: Int) {
	clients(limit: $limit, offset: $offset) {
		id
		userId
		user {
			id
			key
			firstName
			lastName
			firstNameKana
			lastNameKana
			birthDate
			gender
		}
		profile {
			id
			occupation
			hobby
			allowSnsPost
			exerciseHistory
		}
	}
}
